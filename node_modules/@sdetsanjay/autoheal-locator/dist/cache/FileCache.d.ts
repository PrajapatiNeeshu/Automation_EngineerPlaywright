import { SelectorCache, CacheMetrics } from '../core/SelectorCache';
import { CachedSelector } from '../models/CachedSelector';
/**
 * Persistent file-based cache implementation
 */
export declare class FileCache implements SelectorCache {
    private cacheDirectory;
    private cacheFile;
    private cache;
    private hitCount;
    private missCount;
    private evictionCount;
    private maxSize;
    private ttlMs;
    constructor(cacheDirectory?: string, maxSize?: number, ttlMs?: number);
    private ensureCacheDirectory;
    private loadFromDisk;
    private saveToDisk;
    get(key: string): CachedSelector | undefined;
    put(key: string, selector: CachedSelector): void;
    remove(key: string): boolean;
    clearAll(): void;
    size(): number;
    updateSuccess(key: string, success: boolean): void;
    evictExpired(): void;
    getMetrics(): CacheMetrics;
}
//# sourceMappingURL=FileCache.d.ts.map