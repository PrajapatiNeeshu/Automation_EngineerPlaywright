import { AIService, AIAnalysisResult } from '../core/AIService';
import { AutomationFramework } from '../models/AutomationFramework';
/**
 * DeepSeek AI Service implementation
 */
export declare class DeepSeekService implements AIService {
    private readonly apiKey;
    private readonly model;
    private readonly timeout;
    private readonly axiosInstance;
    constructor(apiKey: string, model?: string, timeout?: number);
    analyzeDOM(html: string, description: string, originalSelector: string, framework?: AutomationFramework): Promise<AIAnalysisResult>;
    analyzeVisual(screenshot: Buffer, description: string): Promise<AIAnalysisResult>;
    selectBestMatchingElement(elements: any[], _description: string): Promise<any>;
    getProviderName(): string;
    private buildDOMPrompt;
    private buildVisualPrompt;
    private parseAIResponse;
}
//# sourceMappingURL=DeepSeekService.d.ts.map