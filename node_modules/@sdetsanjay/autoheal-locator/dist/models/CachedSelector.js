"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CachedSelector = void 0;
/**
 * Cached selector with success tracking
 */
class CachedSelector {
    constructor(selector, fingerprint) {
        this.successCount = 0;
        this.failureCount = 0;
        this.selector = selector;
        this.fingerprint = fingerprint;
        this.timestamp = Date.now();
    }
    /**
     * Get current success rate
     */
    getCurrentSuccessRate() {
        const total = this.successCount + this.failureCount;
        return total === 0 ? 1.0 : this.successCount / total;
    }
    /**
     * Update success statistics
     */
    updateSuccess(success) {
        if (success) {
            this.successCount++;
        }
        else {
            this.failureCount++;
        }
    }
}
exports.CachedSelector = CachedSelector;
//# sourceMappingURL=CachedSelector.js.map