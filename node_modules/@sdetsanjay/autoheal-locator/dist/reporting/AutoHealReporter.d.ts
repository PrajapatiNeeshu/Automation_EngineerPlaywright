import { LocatorStrategy } from '../models/LocatorStrategy';
/**
 * AutoHeal Reporter - Tracks and reports all selector usage and healing strategies
 *
 * Generates comprehensive HTML, JSON, and Text reports similar to the Java implementation
 */
export declare class AutoHealReporter {
    private reports;
    private testRunId;
    private startTime;
    private aiProvider;
    private aiModel;
    private apiEndpoint;
    private domTemperature;
    private visualTemperature;
    private domMaxTokens;
    private visualMaxTokens;
    private maxRetries;
    constructor(aiConfig?: any);
    /**
     * Get the API endpoint URL for a given provider
     */
    private getApiEndpointForProvider;
    /**
     * Record a selector usage event
     */
    recordSelectorUsage(originalSelector: string, description: string, strategy: LocatorStrategy, executionTimeMs: number, success: boolean, actualSelector: string, elementDetails: string, reasoning: string, tokensUsed?: number): void;
    private logToConsole;
    private getStrategyShortName;
    /**
     * Generate comprehensive HTML report
     */
    generateHTMLReport(outputDir?: string): string;
    /**
     * Generate JSON report for programmatic consumption
     */
    generateJSONReport(outputDir?: string): string;
    /**
     * Generate text report for easy reading
     */
    generateTextReport(outputDir?: string): string;
    /**
     * Print summary to console
     */
    printSummary(): void;
    private generateHTMLContent;
    private generateAISectionHTML;
    private generateReportRow;
    private generateJavaScriptFilter;
    private generateTextContent;
    private calculateStatistics;
    private getStrategyIcon;
    private getStrategyDisplayName;
    private getRowClass;
    private estimateCost;
    private formatDateTime;
    private formatDuration;
    private escapeHtml;
    private ensureDirectoryExists;
}
//# sourceMappingURL=AutoHealReporter.d.ts.map