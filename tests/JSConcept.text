Object Constructor kya hota hai? (Simple words)

👉 Object Constructor ek special function hota hai
jo objects banane ka template ka kaam karta hai.

Jaise ek mold hota hai jisse same shape ke kai items bante hain.

🔹 Real-Life Example (Samajhne ke liye)

Socho ek Student form hai:

Har student ke paas:

name

age

course

Har baar alag-alag values hongi
lekin structure same rahega.

🔹 JavaScript me Constructor Function kaise likhte hain?
Rule yaad rakho:

Function ka naam Capital letter se

new keyword ka use

this keyword se values set

🔹 Simple Code Example
function Student(name, age, course) {
  this.name = name;
  this.age = age;
  this.course = course;
}


☝️ Ye constructor function hai

🔹 Object kaise banta hai? (new keyword)
const student1 = new Student("Rahul", 22, "JavaScript");
const student2 = new Student("Amit", 25, "Playwright");

🔹 Output samjho
console.log(student1);


Output:

{
  name: "Rahul",
  age: 22,
  course: "JavaScript"
}


👉 student2 ka data alag hoga, par structure same

🔹 this keyword ka matlab kya hai?

this ka matlab hota hai current object

this.name = name;


👉 Matlab:

jis object ko new se banaya

usi ke andar name set karo

🔹 Constructor use karne ke fayde

✅ Bar-bar same object manually banane ki zarurat nahi
✅ Code clean & reusable
✅ Multiple objects easily ban jaate hain
✅ Interview me plus point

🔹 Without Constructor (❌ ganda tareeka)
const student1 = {
  name: "Rahul",
  age: 22,
  course: "JS"
};

const student2 = {
  name: "Amit",
  age: 25,
  course: "Playwright"
};


👉 Same cheez repeat ho rahi hai

🔹 With Constructor (✅ best practice)
const s1 = new Student("Rahul", 22, "JS");
const s2 = new Student("Amit", 25, "Playwright");

🔹 Interview ke liye 1-line answer

Constructor function JavaScript me ek blueprint hota hai jisse same type ke multiple objects create kiye ja sakte hain using the new keyword.

🔹 Bonus: Constructor me Method kaise add kare?
function Student(name, age) {
  this.name = name;
  this.age = age;

  this.sayHello = function () {
    console.log("Hello, my name is " + this.name);
  };
}

const s1 = new Student("Rahul", 22);
s1.sayHello();

🔹 Final yaad rakhne wali baat
Term	Meaning
Constructor	Object banane ka template
new	Naya object banata hai
this	Current object
Instance	Constructor se bana object